initSidebarItems({"struct":[["Field","Structs that represents the whole Field. Each individual Row is hold in its own Row struct which are hold in the `Vec`. `size` is a the demionsions of the Field. There should be `size` rows and each row should have a length of `size`. "],["Row","Struct that represents one line of the Field. The Vec is always of length `length`. Cells that are currently empty holf an `None`"]]});